# Copiar como functions/.env.<PROJECT_ID>  (ej. functions/.env.rapifix-prod-a1b2c)
# Correo que puede reclamar el primer usuario administrador (solo funciona una vez).
BOOTSTRAP_ADMIN_EMAIL=
TENANT_ID=rapifix
